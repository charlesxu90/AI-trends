name: Monthly data refresh

# Runs the full pipeline (crawl -> process -> assign -> AI curate -> trends ->
# export-site) and opens a PR with any regenerated data/site. Conferences release
# annually, so most monthly runs will produce no diff (and thus no PR).
#
# Requirements:
#   - Secret ANTHROPIC_API_KEY (for AI curation; without it, curation is skipped).
#   - Repo setting: Settings -> Actions -> General -> "Allow GitHub Actions to
#     create and approve pull requests" must be enabled.
#   - Crawl venue/domain strings in config/crawl.json must be current (they change
#     each cycle); a stale/empty crawl simply yields no new data.

on:
  schedule:
    - cron: "0 6 1 * *" # 06:00 UTC on the 1st of each month
  workflow_dispatch:
    inputs:
      crawl:
        description: "Run the OpenReview crawl"
        type: boolean
        default: true
      discover:
        description: "Probe & ingest newly-available conference-years"
        type: boolean
        default: true
      curate:
        description: "Run AI topic curation"
        type: boolean
        default: true

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip "setuptools<81"
          pip install -e . --no-deps
          pip install -r requirements-lock.txt
          pip install scrapy "anthropic>=0.40" "requests>=2.28" "beautifulsoup4>=4.11"
          pip install "https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.1/en_core_sci_lg-0.5.1.tar.gz"

      - name: Resolve stage flags
        id: flags
        env:
          # Pass dispatch inputs via env (never interpolate ${{ }} into the shell body).
          CRAWL_INPUT: ${{ github.event.inputs.crawl }}
          DISCOVER_INPUT: ${{ github.event.inputs.discover }}
          CURATE_INPUT: ${{ github.event.inputs.curate }}
        run: |
          # default to enabled on schedule (no inputs provided)
          [ "$CRAWL_INPUT" = "false" ] && echo "crawl=" >> "$GITHUB_OUTPUT" || echo "crawl=--crawl" >> "$GITHUB_OUTPUT"
          [ "$DISCOVER_INPUT" = "false" ] && echo "discover=" >> "$GITHUB_OUTPUT" || echo "discover=--discover" >> "$GITHUB_OUTPUT"
          [ "$CURATE_INPUT" = "false" ] && echo "curate=" >> "$GITHUB_OUTPUT" || echo "curate=--curate" >> "$GITHUB_OUTPUT"

      - name: Check conference source links (report gaps / dead links)
        run: ai-trend check-sources --check || true

      - name: Run refresh
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          ai-trend refresh ${{ steps.flags.outputs.crawl }} ${{ steps.flags.outputs.discover }} ${{ steps.flags.outputs.curate }} \
            --spacy-model en_core_sci_lg

      - name: Open pull request with refreshed data
        uses: peter-evans/create-pull-request@v6
        with:
          branch: auto/monthly-refresh
          delete-branch: true
          commit-message: "chore: monthly AI-trend data refresh"
          title: "chore: monthly AI-trend data refresh"
          body: |
            Automated monthly refresh: crawl → **discover** (probe & ingest newly-available
            conference-years) → process → assign → AI curate → trends → export-site.

            Review the regenerated taxonomy (`config/`), labels (`data/**_topics.csv`),
            and site (`docs/`). Merge to publish
            (Pages redeploys from `main`/`docs`). Check the "Check conference source links"
            step log for gaps / dead links in `CONFERENCES.md`.
          add-paths: |
            config/**
            data/**
            docs/**
            citation-history/**
            CONFERENCES.md
            README.md
